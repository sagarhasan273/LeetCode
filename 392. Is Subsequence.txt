class Solution {
public:
    bool isSubsequence(string s, string t) {
        int l=s.size(), r=t.size();
        int i = 0;
        for (int j=0; j<r; j++){
            if (s[i] == t[j]) i++;
        }
        
        return i == l;
    }
};
